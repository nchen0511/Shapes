public class Square extends Rectangle{

    /**
     * Extends into rectangle, inputting the same value for length and width
     *
     * @param side
     */
    public Square(int side){
        super(side,side);
    }
}
